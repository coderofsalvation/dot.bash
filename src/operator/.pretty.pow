.pretty()
  local str=$(cat)
  # check whether json
  local first=${str:0:1}
  local last=${str:$((${#str}-1))}
  if $first == "{" || $first == "["       
    if $last == "}" || $last == "]"       
      echo "$str" | json_pp
      return 0
  echo "$str"
